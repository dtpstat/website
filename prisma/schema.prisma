generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Comment {
  id          Int       @id @default(autoincrement())
  text        String?
  createDate  DateTime? @db.Date @default(now())
  authorId    String    @db.VarChar
  isPublished Boolean   @default(false)
  author      User      @relation(fields: [authorId], references: [auth0userSub])
}

model User {
  name         String?
  email        String?
  avatarUrl    String?
  auth0userSub String    @id @db.VarChar
  comments     Comment[]
}
